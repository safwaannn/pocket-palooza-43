# Paisa — local development environment
# Copy .env.example → .env and fill in your values.

# Base URL of the Express/MongoDB backend. Vite only exposes VITE_-prefixed vars
# to the client bundle.
VITE_API_URL=http://localhost:3000

NODE_ENV=development
PORT=5173
